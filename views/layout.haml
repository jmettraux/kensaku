!!!
%html

  %head

    %title kensaku

    %meta{ :charset => 'utf-8' }
    %meta{ :name => 'apple-mobile-web-app-capable', :content => 'yes' }
    %meta{ :name => 'viewport', :content => 'user-scalable=no, width=device-width, initial-scale=1.0, maximum-scale=1.0' }

    %link{ :rel => 'stylesheet', :href => 'style.css', :type => 'text/css' }
    %script{ :src => '/js/caboche-1.2.2.min.js' }

  %body

    = yield

    :javascript

      Caboche.phase(0, 'http://code.jquery.com/jquery-1.10.1.min.js');
      Caboche.phase(0, '/js/foolbox-all-1.0.0.min.js');

      Caboche.phase(1, function() { Fb = Foolbox; });
      Caboche.phase(1, '/js/half-0.9.0.min.js');

      Caboche.last(function() {
        console.log("done.");
      });

